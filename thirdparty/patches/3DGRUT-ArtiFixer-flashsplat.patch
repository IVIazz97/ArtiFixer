diff --git a/configs/base_gs_sparse.yaml b/configs/base_gs_sparse.yaml
index 556016b..7e55bb9 100644
--- a/configs/base_gs_sparse.yaml
+++ b/configs/base_gs_sparse.yaml
@@ -9,4 +9,5 @@ novel_path: null
 selected_indices_file: null
 train_test_split_file: null
 image_path_override: null
+object_mask_path_override: null
 num_selected_indices: 1000000
\ No newline at end of file
diff --git a/setup.py b/setup.py
index 7caa085..21229aa 100644
--- a/setup.py
+++ b/setup.py
@@ -24,8 +24,19 @@ setup(
     author_email="nicolasm@nvidia.com",
     description="Official implementation of 3DGRT and 3DGUT research projects.",
     long_description_content_type="text/markdown",
-    py_modules=["threedgrut", "threedgrt_tracer", "threedgut_tracer", "playground"],
-    packages=find_packages(where="libs"),
+    py_modules=["playground"],
+    packages=find_packages(include=[
+        "threedgrut",
+        "threedgrut.*",
+        "threedgrt_tracer",
+        "threedgrt_tracer.*",
+        "threedgut_tracer",
+        "threedgut_tracer.*",
+        "threedgrut_flashsplat_tracer",
+        "threedgrut_flashsplat_tracer.*",
+        "threedgrut_playground",
+        "threedgrut_playground.*",
+    ]),
     python_requires=">=3.11",
     install_requires=[],
     classifiers=["Operating System :: OS Independent"],
diff --git a/threedgrut/datasets/__init__.py b/threedgrut/datasets/__init__.py
index b70b437..17cb5d9 100644
--- a/threedgrut/datasets/__init__.py
+++ b/threedgrut/datasets/__init__.py
@@ -43,6 +43,7 @@ def make(name: str, config, ray_jitter):
                 num_selected_indices=config.num_selected_indices, # Number of selected camera indices for sparse recon
                 train_test_split_file=config.train_test_split_file, # For mipnerf360 ReconFusion, a json file with train-test camera poses
                 image_path_override=config.image_path_override, # Override the image path (to distill renderings in training)
+                object_mask_path_override=getattr(config, "object_mask_path_override", None), # Optional object-id mask directory
             )
             val_dataset = ColmapDataset(
                 config.path,
@@ -53,6 +54,7 @@ def make(name: str, config, ray_jitter):
                 num_selected_indices=config.num_selected_indices, # Number of selected camera indices for sparse recon
                 train_test_split_file=config.train_test_split_file, # For mipnerf360 ReconFusion, a json file with train-test camera poses
                 image_path_override=config.image_path_override, # Override the image path (to distill renderings in training)
+                object_mask_path_override=getattr(config, "object_mask_path_override", None), # Optional object-id mask directory
             )
         case "scannetpp":
             train_dataset = ScannetppDataset(
@@ -93,6 +95,7 @@ def make_test(name: str, config):
                 selected_indices_file=config.selected_indices_file, # A json file with the first (or second) half ordered camera poses
                 num_selected_indices=config.num_selected_indices, # Number of selected camera indices for sparse recon
                 train_test_split_file=config.train_test_split_file, # For mipnerf360 ReconFusion, a json file with train-test camera poses
+                object_mask_path_override=getattr(config, "object_mask_path_override", None), # Optional object-id mask directory
             )
         case "scannetpp":
             dataset = ScannetppDataset(
diff --git a/threedgrut/datasets/dataset_colmap.py b/threedgrut/datasets/dataset_colmap.py
index aaff6c9..38c8c0f 100644
--- a/threedgrut/datasets/dataset_colmap.py
+++ b/threedgrut/datasets/dataset_colmap.py
@@ -60,6 +60,7 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
         num_selected_indices=None, # Number of selected camera indices for sparse recon
         train_test_split_file=None, # For mipnerf360 ReconFusion, a json file with train-test camera poses
         image_path_override=None, # Override the image path
+        object_mask_path_override=None, # Optional object-id mask path override (frame-indexed PNG/NPY labels)
     ):
         self.path = path
         self.device = device
@@ -71,6 +72,7 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
         self.num_selected_indices=num_selected_indices
         self.train_test_split_file=train_test_split_file
         self.image_path_override=image_path_override
+        self.object_mask_path_override = object_mask_path_override
         # Worker-based GPU cache for multiprocessing compatibility
         self._worker_gpu_cache = {}
 
@@ -140,6 +142,7 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
         self.poses = self.poses[indices].astype(np.float32)
         self.w2cs = self.w2cs[indices]
         self.image_paths = self.image_paths[indices]  # numpy str array of image paths
+        self.object_mask_paths = self.object_mask_paths[indices]  # numpy str array of object-id mask paths
         self.camera_centers = self.camera_centers[indices]
         self.is_override_flags = self.is_override_flags[indices]  # Filter override flags by split
         self.center, self.length_scale, self.scene_bbox = self.compute_spatial_extents()
@@ -419,6 +422,7 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
         self.w2cs = []
         self.image_paths = []
         self.mask_paths = []
+        self.object_mask_paths = []
 
         cam_centers = []
         for frame_idx, extr in enumerate(logger.track(
@@ -450,6 +454,12 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
 
                 # Mask path
                 self.mask_paths.append(os.path.splitext(image_path)[0] + "_mask.png" if image_path else "")
+                if self.object_mask_path_override:
+                    self.object_mask_paths.append(
+                        os.path.join(self.path, self.object_mask_path_override, f"{frame_idx:05d}.png")
+                    )
+                else:
+                    self.object_mask_paths.append("")
             else:
                 R = qvec_to_so3(extr.qvec)
                 T = np.array(extr.tvec)
@@ -472,6 +482,12 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
 
                 # Mask path
                 self.mask_paths.append(os.path.splitext(image_path)[0] + "_mask.png")
+                if self.object_mask_path_override:
+                    self.object_mask_paths.append(
+                        os.path.join(self.path, self.object_mask_path_override, f"{frame_idx:05d}.png")
+                    )
+                else:
+                    self.object_mask_paths.append("")
 
         self.camera_centers = np.array(cam_centers)
         _, diagonal = get_center_and_diag(self.camera_centers)
@@ -481,6 +497,7 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
         self.w2cs = np.stack(self.w2cs)
         self.image_paths = np.stack(self.image_paths, dtype=str)
         self.mask_paths = np.stack(self.mask_paths, dtype=str)
+        self.object_mask_paths = np.stack(self.object_mask_paths, dtype=str)
 
         # Track which frames use override images (for applying different loss)
         self.is_override_flags = np.array([
@@ -566,6 +583,76 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
     def __len__(self) -> int:
         return self.n_frames
 
+    def _object_mask_root_dir(self) -> str | None:
+        if not self.object_mask_path_override:
+            return None
+        if os.path.isabs(self.object_mask_path_override):
+            return self.object_mask_path_override
+        return os.path.join(self.path, self.object_mask_path_override)
+
+    def _candidate_object_mask_paths(self, idx: int) -> list[str]:
+        candidates = []
+        configured_path = self.object_mask_paths[idx]
+        if configured_path:
+            candidates.append(configured_path)
+            if configured_path.lower().endswith(".png"):
+                candidates.append(configured_path[:-4] + ".npy")
+
+        root_dir = self._object_mask_root_dir()
+        if root_dir is not None:
+            image_stem = ""
+            image_path = self.image_paths[idx]
+            if image_path:
+                image_stem = os.path.splitext(os.path.basename(image_path))[0]
+
+            stems = [
+                image_stem,
+                f"{idx:05d}",
+                f"{idx + 1:05d}",
+                f"frame_{idx:05d}",
+                f"frame_{idx + 1:05d}",
+            ]
+            for stem in stems:
+                if not stem:
+                    continue
+                candidates.append(os.path.join(root_dir, f"{stem}.png"))
+                candidates.append(os.path.join(root_dir, f"{stem}.npy"))
+
+        seen = set()
+        existing = []
+        for path in candidates:
+            if path and path not in seen:
+                seen.add(path)
+                if os.path.exists(path):
+                    existing.append(path)
+        return existing
+
+    @staticmethod
+    def _load_object_mask_array(mask_path: str) -> np.ndarray:
+        if mask_path.lower().endswith(".npy"):
+            object_mask = np.load(mask_path, allow_pickle=False)
+        else:
+            object_mask = np.array(Image.open(mask_path).convert("L"))
+
+        if object_mask.ndim == 3 and object_mask.shape[-1] == 1:
+            object_mask = object_mask[..., 0]
+        elif object_mask.ndim == 3:
+            object_mask = object_mask[..., 0]
+        if object_mask.ndim != 2:
+            raise ValueError(f"Object mask at {mask_path} must be 2D, got shape {object_mask.shape}")
+
+        if object_mask.dtype == np.bool_:
+            object_mask = object_mask.astype(np.uint8)
+        elif np.issubdtype(object_mask.dtype, np.floating):
+            if object_mask.max(initial=0.0) <= 1.0:
+                object_mask = np.rint(object_mask).astype(np.uint8)
+            else:
+                object_mask = np.rint(object_mask).astype(np.int32)
+        elif not np.issubdtype(object_mask.dtype, np.integer):
+            object_mask = object_mask.astype(np.int32)
+
+        return object_mask
+
     def __getitem__(self, idx) -> dict:
         # Load image and get its actual dimensions
         image_path = self.image_paths[idx]
@@ -598,6 +685,20 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
             mask = torch.from_numpy(np.array(Image.open(mask_path).convert("L"))).reshape(1, actual_h, actual_w, 1)
             output_dict["mask"] = mask
 
+        # Object-ID masks are kept as raw labels (no binarization) for FlashSplat-style accumulation.
+        object_mask_candidates = self._candidate_object_mask_paths(idx)
+        if object_mask_candidates:
+            object_mask_path = object_mask_candidates[0]
+            object_mask_array = self._load_object_mask_array(object_mask_path)
+            object_mask = torch.from_numpy(object_mask_array)
+            if object_mask.shape != (actual_h, actual_w):
+                object_mask = torch.nn.functional.interpolate(
+                    object_mask.to(torch.float32).unsqueeze(0).unsqueeze(0),
+                    size=(actual_h, actual_w),
+                    mode="nearest",
+                ).squeeze(0).squeeze(0)
+            output_dict["object_mask"] = object_mask.reshape(1, actual_h, actual_w, 1)
+
         return output_dict
 
     def get_gpu_batch_with_intrinsics(self, batch):
@@ -634,6 +735,10 @@ class ColmapDataset(Dataset, BoundedMultiViewDataset, DatasetVisualization):
             mask = (mask > 0.5).to(torch.float32)
             sample["mask"] = mask
 
+        if "object_mask" in batch:
+            object_mask = batch["object_mask"][0].to(self.device, non_blocking=True).to(torch.float32)
+            sample["object_mask"] = object_mask
+
         return Batch(**sample)
 
     def create_dataset_camera_visualization(self):
diff --git a/threedgrut/datasets/protocols.py b/threedgrut/datasets/protocols.py
index 0b11a41..fb236b0 100644
--- a/threedgrut/datasets/protocols.py
+++ b/threedgrut/datasets/protocols.py
@@ -27,6 +27,8 @@ class Batch:
     T_to_world: torch.Tensor  # [B, 4, 4] transformation matrix from the ray space to the world space
     rgb_gt: Optional[torch.Tensor] = None
     mask: Optional[torch.Tensor] = None
+    object_mask: Optional[torch.Tensor] = None
+    unique_label: Optional[torch.Tensor] = None
     intrinsics: Optional[list] = None
     intrinsics_OpenCVPinholeCameraModelParameters: Optional[dict] = None
     intrinsics_OpenCVFisheyeCameraModelParameters: Optional[dict] = None
@@ -42,6 +44,9 @@ class Batch:
         if self.mask is not None:
             assert self.mask.ndim == 4, "mask must be a 3D tensor [B, H, W, 1]"
             assert self.mask.shape[0] == batch_size, "mask must have the same batch size"
+        if self.object_mask is not None:
+            assert self.object_mask.ndim == 4, "object_mask must be a 4D tensor [B, H, W, 1]"
+            assert self.object_mask.shape[0] == batch_size, "object_mask must have the same batch size"
         if self.intrinsics:
             assert isinstance(self.intrinsics, list), "intrinsics must be a list"
             assert len(self.intrinsics) == 4, "intrinsics must have 4 elements [fx, fy, cx, cy]"
diff --git a/threedgrut/model/model.py b/threedgrut/model/model.py
index 05e69bf..f14474b 100644
--- a/threedgrut/model/model.py
+++ b/threedgrut/model/model.py
@@ -187,6 +187,9 @@ class MixtureOfGaussians(torch.nn.Module, ExportableModel):
             self.renderer = threedgrt_tracer.Tracer(conf)
         elif conf.render.method == "3dgut":
             self.renderer = threedgut_tracer.Tracer(conf)
+        elif conf.render.method == "threedgrut_flashsplat":
+            # Keep the method name for compatibility, but route through native 3DGUT rasterization.
+            self.renderer = threedgut_tracer.Tracer(conf)
         else:
             raise ValueError(f"Unknown rendering method: {conf.render.method}")
 
diff --git a/threedgut_tracer/bindings.cpp b/threedgut_tracer/bindings.cpp
index 1f02ffe..8302762 100644
--- a/threedgut_tracer/bindings.cpp
+++ b/threedgut_tracer/bindings.cpp
@@ -81,6 +81,7 @@ PYBIND11_MODULE(TORCH_EXTENSION_NAME, m) {
     pybind11::class_<SplatRaster>(m, "SplatRaster")
         .def(pybind11::init<const nlohmann::json&>())
         .def("trace", &SplatRaster::trace)
+        .def("trace_with_object_mask", &SplatRaster::traceWithObjectMask)
         .def("trace_bwd", &SplatRaster::traceBwd)
         .def("collect_times", &SplatRaster::collectTimes);
 
diff --git a/threedgut_tracer/include/3dgut/kernels/cuda/renderers/gutKBufferRenderer.cuh b/threedgut_tracer/include/3dgut/kernels/cuda/renderers/gutKBufferRenderer.cuh
index c05c7c3..3ffa657 100644
--- a/threedgut_tracer/include/3dgut/kernels/cuda/renderers/gutKBufferRenderer.cuh
+++ b/threedgut_tracer/include/3dgut/kernels/cuda/renderers/gutKBufferRenderer.cuh
@@ -18,6 +18,8 @@
 #include <3dgut/kernels/cuda/common/rayPayloadBackward.cuh>
 #include <3dgut/renderer/gutRendererParameters.h>
 
+#include <cstdint>
+
 struct HitParticle {
     static constexpr float InvalidHitT = -1.0f;
     int idx                            = -1;
@@ -111,7 +113,11 @@ struct GUTKBufferRenderer : Params {
         const HitParticle& hitParticle,
         const Particles& particles,
         const TFeaturesVec* __restrict__ particleFeatures,
-        TFeaturesVec* __restrict__ particleFeaturesGradient) {
+        TFeaturesVec* __restrict__ particleFeaturesGradient,
+        const int32_t* __restrict__ objectMaskPtr,
+        int objectMaskNumObj,
+        float* __restrict__ objectGaussianAccumPtr,
+        uint32_t numParticles) {
 
         if constexpr (Backward) {
             float hitAlphaGrad = 0.f;
@@ -161,6 +167,13 @@ struct GUTKBufferRenderer : Params {
                                           Params::PerRayParticleFeatures ? particles.featuresFromBuffer(hitParticle.idx, ray.direction) : tcnn::max(particleFeatures[hitParticle.idx], 0.f),
                                           ray.features);
 
+            if (objectMaskPtr != nullptr && objectGaussianAccumPtr != nullptr && objectMaskNumObj > 0) {
+                const int32_t objId = objectMaskPtr[ray.idx];
+                if (objId >= 0 && objId < objectMaskNumObj) {
+                    atomicAdd(&objectGaussianAccumPtr[static_cast<uint64_t>(objId) * numParticles + hitParticle.idx], hitWeight);
+                }
+            }
+
             if (hitWeight > 0.0f) ray.countHit();
         }
 
@@ -174,6 +187,10 @@ struct GUTKBufferRenderer : Params {
                                        TRay& ray,
                                        const tcnn::uvec2* __restrict__ sortedTileRangeIndicesPtr,
                                        const uint32_t* __restrict__ sortedTileParticleIdxPtr,
+                                       const int32_t* __restrict__ objectMaskPtr,
+                                       int objectMaskNumObj,
+                                       float* __restrict__ objectGaussianAccumPtr,
+                                       uint32_t numParticles,
                                        const tcnn::vec2* __restrict__ /*particlesProjectedPositionPtr*/,
                                        const tcnn::vec4* __restrict__ /*particlesProjectedConicOpacityPtr*/,
                                        const float* __restrict__ /*particlesGlobalDepthPtr*/,
@@ -210,7 +227,8 @@ struct GUTKBufferRenderer : Params {
                                   sortedTileParticleIdxPtr, particleFeaturesBuffer, particleFeaturesGradientBuffer);
         } else {
             evalKBuffer(ray, particles, tileParticleRangeIndices, tileNumBlocksToProcess, tileNumParticlesToProcess, tileThreadIdx,
-                        sortedTileParticleIdxPtr, particleFeaturesBuffer, particleFeaturesGradientBuffer);
+                        sortedTileParticleIdxPtr, particleFeaturesBuffer, particleFeaturesGradientBuffer,
+                        objectMaskPtr, objectMaskNumObj, objectGaussianAccumPtr, numParticles);
         }
     }
 
@@ -223,7 +241,11 @@ struct GUTKBufferRenderer : Params {
                                               const uint32_t tileThreadIdx,
                                               const uint32_t* __restrict__ sortedTileParticleIdxPtr,
                                               const TFeaturesVec* __restrict__ particleFeaturesBuffer,
-                                              TFeaturesVec* __restrict__ particleFeaturesGradientBuffer) {
+                                              TFeaturesVec* __restrict__ particleFeaturesGradientBuffer,
+                                              const int32_t* __restrict__ objectMaskPtr,
+                                              int objectMaskNumObj,
+                                              float* __restrict__ objectGaussianAccumPtr,
+                                              uint32_t numParticles) {
         using namespace threedgut;
         __shared__ PrefetchedParticleData prefetchedParticlesData[GUTParameters::Tiling::BlockSize];
 
@@ -273,7 +295,11 @@ struct GUTKBufferRenderer : Params {
                                            hitParticleKBuffer.closestHit(hitParticle),
                                            particles,
                                            particleFeaturesBuffer,
-                                           particleFeaturesGradientBuffer);
+                                           particleFeaturesGradientBuffer,
+                                           objectMaskPtr,
+                                           objectMaskNumObj,
+                                           objectGaussianAccumPtr,
+                                           numParticles);
                     }
                     hitParticleKBuffer.insert(hitParticle);
                 }
@@ -286,7 +312,11 @@ struct GUTKBufferRenderer : Params {
                                    hitParticleKBuffer[Params::KHitBufferSize - hitParticleKBuffer.numHits() + i],
                                    particles,
                                    particleFeaturesBuffer,
-                                   particleFeaturesGradientBuffer);
+                                   particleFeaturesGradientBuffer,
+                                   objectMaskPtr,
+                                   objectMaskNumObj,
+                                   objectGaussianAccumPtr,
+                                   numParticles);
             }
         }
     }
@@ -298,6 +328,10 @@ struct GUTKBufferRenderer : Params {
                                        TRay& ray,
                                        const tcnn::uvec2* __restrict__ sortedTileRangeIndicesPtr,
                                        const uint32_t* __restrict__ sortedTileParticleIdxPtr,
+                                       const int32_t* __restrict__ objectMaskPtr,
+                                       int objectMaskNumObj,
+                                       float* __restrict__ objectGaussianAccumPtr,
+                                       uint32_t numParticles,
                                        const tcnn::vec2* __restrict__ particlesProjectedPositionPtr,
                                        const tcnn::vec4* __restrict__ particlesProjectedConicOpacityPtr,
                                        const float* __restrict__ particlesGlobalDepthPtr,
@@ -353,11 +387,13 @@ struct GUTKBufferRenderer : Params {
             float hitT = 0.0f;
             TFeaturesVec hitFeatures = TFeaturesVec::zero();
             bool validHit = false;
+            uint32_t hitParticleIdx = GUTParameters::InvalidParticleIdx;
 
             // Step 1: Each thread tests one Gaussian intersection
             if (j < tileNumParticlesToProcess) {
                 const uint32_t toProcessSortedIndex = tileParticleRangeIndices.x + j;
                 const uint32_t particleIdx = sortedTileParticleIdxPtr[toProcessSortedIndex];
+                hitParticleIdx = particleIdx;
 
                 if (particleIdx != GUTParameters::InvalidParticleIdx) {
                     auto densityParams = particles.fetchDensityParameters(particleIdx);
@@ -429,6 +465,13 @@ struct GUTKBufferRenderer : Params {
                 float particleTransmittance = ray.transmittance * prefixTransmittance;
                 float hitWeight = hitAlpha * particleTransmittance;
 
+                if (objectMaskPtr != nullptr && objectGaussianAccumPtr != nullptr && objectMaskNumObj > 0 && hitWeight > 0.0f && hitParticleIdx != GUTParameters::InvalidParticleIdx) {
+                    const int32_t objId = objectMaskPtr[ray.idx];
+                    if (objId >= 0 && objId < objectMaskNumObj) {
+                        atomicAdd(&objectGaussianAccumPtr[static_cast<uint64_t>(objId) * numParticles + hitParticleIdx], hitWeight);
+                    }
+                }
+
                 // Compute weighted contributions
                 for (int featIdx = 0; featIdx < Particles::FeaturesDim; ++featIdx) {
                     accumulatedFeatures[featIdx] = hitFeatures[featIdx] * hitWeight;
diff --git a/threedgut_tracer/include/3dgut/kernels/cuda/renderers/gutRenderer.cuh b/threedgut_tracer/include/3dgut/kernels/cuda/renderers/gutRenderer.cuh
index 0a05354..243b9f6 100644
--- a/threedgut_tracer/include/3dgut/kernels/cuda/renderers/gutRenderer.cuh
+++ b/threedgut_tracer/include/3dgut/kernels/cuda/renderers/gutRenderer.cuh
@@ -18,6 +18,8 @@
 #include <3dgut/kernels/cuda/common/rayPayload.cuh>
 #include <3dgut/renderer/gutRendererParameters.h>
 
+#include <cstdint>
+
 __global__ void projectOnTiles(tcnn::uvec2 tileGrid,
                                uint32_t numParticles,
                                tcnn::ivec2 resolution,
@@ -89,6 +91,10 @@ __global__ void render(threedgut::RenderParameters params,
                        float* __restrict__ worldHitCountPtr,
                        float* __restrict__ worldHitDistancePtr,
                        tcnn::vec4* __restrict__ radianceDensityPtr,
+                       const int32_t* __restrict__ objectMaskPtr,
+                       int objectMaskNumObj,
+                       float* __restrict__ objectGaussianAccumPtr,
+                       uint32_t numParticles,
                        const tcnn::vec2* __restrict__ particlesProjectedPositionPtr,
                        const tcnn::vec4* __restrict__ particlesProjectedConicOpacityPtr,
                        const float* __restrict__ particlesGlobalDepthPtr,
@@ -102,6 +108,10 @@ __global__ void render(threedgut::RenderParameters params,
                        ray,
                        sortedTileRangeIndicesPtr,
                        sortedTileDataPtr,
+                       objectMaskPtr,
+                       objectMaskNumObj,
+                       objectGaussianAccumPtr,
+                       numParticles,
                        particlesProjectedPositionPtr,
                        particlesProjectedConicOpacityPtr,
                        particlesGlobalDepthPtr,
@@ -124,6 +134,10 @@ __global__ void renderBalanced(threedgut::RenderParameters params,
                                        float* __restrict__ worldHitCountPtr,
                                        float* __restrict__ worldHitDistancePtr,
                                        tcnn::vec4* __restrict__ radianceDensityPtr,
+                                       const int32_t* __restrict__ objectMaskPtr,
+                                       int objectMaskNumObj,
+                                       float* __restrict__ objectGaussianAccumPtr,
+                                       uint32_t numParticles,
                                        const tcnn::vec2* __restrict__ particlesProjectedPositionPtr,
                                        const tcnn::vec4* __restrict__ particlesProjectedConicOpacityPtr,
                                        const float* __restrict__ particlesGlobalDepthPtr,
@@ -196,6 +210,10 @@ __global__ void renderBalanced(threedgut::RenderParameters params,
                                             ray,
                                             sortedTileRangeIndicesPtr,
                                             sortedTileDataPtr,
+                                            objectMaskPtr,
+                                            objectMaskNumObj,
+                                            objectGaussianAccumPtr,
+                                            numParticles,
                                             particlesProjectedPositionPtr,
                                             particlesProjectedConicOpacityPtr,
                                             particlesGlobalDepthPtr,
@@ -252,6 +270,10 @@ __global__ void renderBackward(threedgut::RenderParameters params,
                                ray,
                                sortedTileRangeIndicesPtr,
                                sortedTileDataPtr,
+                               nullptr,
+                               0,
+                               nullptr,
+                               0,
                                particlesProjectedPositionPtr,
                                particlesProjectedConicOpacityPtr,
                                particlesGlobalDepthPtr,
diff --git a/threedgut_tracer/include/3dgut/renderer/gutRenderer.h b/threedgut_tracer/include/3dgut/renderer/gutRenderer.h
index 7abd81f..d3d54e1 100644
--- a/threedgut_tracer/include/3dgut/renderer/gutRenderer.h
+++ b/threedgut_tracer/include/3dgut/renderer/gutRenderer.h
@@ -20,6 +20,7 @@
 
 #include <json/json.hpp>
 
+#include <cstdint>
 #include <memory>
 
 namespace threedgut {
@@ -69,6 +70,9 @@ public:
                          float* worldHitDistanceCudaPtr,
                          tcnn::vec4* radianceDensityCudaPtr,
                          int* particlesVisibilityCudaPtr,
+                         const int32_t* objectMaskCudaPtr,
+                         int objectMaskNumObj,
+                         float* objectGaussianAccumCudaPtr,
                          Parameters& parameters,
                          int cudaDeviceIndex,
                          cudaStream_t cudaStream);
diff --git a/threedgut_tracer/include/3dgut/splatRaster.h b/threedgut_tracer/include/3dgut/splatRaster.h
index 48dfbb8..58f1546 100644
--- a/threedgut_tracer/include/3dgut/splatRaster.h
+++ b/threedgut_tracer/include/3dgut/splatRaster.h
@@ -64,6 +64,25 @@ public:
           torch::Tensor sensorsStartPose,
           torch::Tensor sensorsEndPose);
 
+    std::tuple<torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>
+    traceWithObjectMask(uint32_t frameNumber, int numActiveFeatures,
+                        // Particles
+                        torch::Tensor particleDensity,
+                        torch::Tensor particleRadiance,
+                        // Rays
+                        torch::Tensor rayOrigin,
+                        torch::Tensor rayDirection,
+                        torch::Tensor rayTimestamp,
+                        // Sensor
+                        threedgut::TSensorModel sensor,
+                        TTimestamp startTimestamp,
+                        TTimestamp endTimestamp,
+                        torch::Tensor sensorsStartPose,
+                        torch::Tensor sensorsEndPose,
+                        // Object mask and accumulation config
+                        torch::Tensor objectMask,
+                        int objectMaskNumObj);
+
     std::tuple<torch::Tensor, torch::Tensor>
     traceBwd(uint32_t frameNumber, int numActiveFeatures,
              // Particles
diff --git a/threedgut_tracer/src/gutRenderer.cu b/threedgut_tracer/src/gutRenderer.cu
index 082b8da..e00a6e4 100644
--- a/threedgut_tracer/src/gutRenderer.cu
+++ b/threedgut_tracer/src/gutRenderer.cu
@@ -30,6 +30,7 @@
 #include <cub/cub.cuh>
 #include <cub/device/device_radix_sort.cuh>
 
+#include <cstdint>
 #include <limits>
 
 using namespace tcnn;
@@ -245,6 +246,9 @@ threedgut::Status threedgut::GUTRenderer::renderForward(const RenderParameters&
                                                         float* worldHitDistanceCudaPtr,
                                                         vec4* radianceDensityCudaPtr,
                                                         int* particlesVisibilityCudaPtr,
+                                                        const int32_t* objectMaskCudaPtr,
+                                                        int objectMaskNumObj,
+                                                        float* objectGaussianAccumCudaPtr,
                                                         Parameters& parameters,
                                                         int cudaDeviceIndex,
                                                         cudaStream_t cudaStream) {
@@ -391,6 +395,10 @@ threedgut::Status threedgut::GUTRenderer::renderForward(const RenderParameters&
             worldHitCountCudaPtr,
             worldHitDistanceCudaPtr,
             radianceDensityCudaPtr,
+            objectMaskCudaPtr,
+            objectMaskNumObj,
+            objectGaussianAccumCudaPtr,
+            numParticles,
             (const tcnn::vec2*)m_forwardContext->particlesProjectedPosition.data(),
             (const tcnn::vec4*)m_forwardContext->particlesProjectedConicOpacity.data(),
             (const float*)m_forwardContext->particlesGlobalDepth.data(),
@@ -409,6 +417,10 @@ threedgut::Status threedgut::GUTRenderer::renderForward(const RenderParameters&
             worldHitCountCudaPtr,
             worldHitDistanceCudaPtr,
             radianceDensityCudaPtr,
+            objectMaskCudaPtr,
+            objectMaskNumObj,
+            objectGaussianAccumCudaPtr,
+            numParticles,
             (const tcnn::vec2*)m_forwardContext->particlesProjectedPosition.data(),
             (const tcnn::vec4*)m_forwardContext->particlesProjectedConicOpacity.data(),
             (const float*)m_forwardContext->particlesGlobalDepth.data(),
diff --git a/threedgut_tracer/src/splatRaster.cpp b/threedgut_tracer/src/splatRaster.cpp
index f277acd..0ea5658 100644
--- a/threedgut_tracer/src/splatRaster.cpp
+++ b/threedgut_tracer/src/splatRaster.cpp
@@ -230,6 +230,9 @@ SplatRaster::trace(uint32_t frameNumber, int numActiveFeatures,
         reinterpret_cast<float*>(voidDataPtr(rayHitDistance)),
         reinterpret_cast<tcnn::vec4*>(voidDataPtr(rayRadianceDensity)),
         reinterpret_cast<int*>(voidDataPtr(particleVisibility)),
+        nullptr,
+        0,
+        nullptr,
         m_parameters,
         cudaDeviceIndex,
         cudaStream);
@@ -243,6 +246,100 @@ SplatRaster::trace(uint32_t frameNumber, int numActiveFeatures,
     return std::tuple<torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>(rayRadianceDensity, rayHitDistance, rayHitCount, particleVisibility);
 }
 
+std::tuple<torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>
+SplatRaster::traceWithObjectMask(uint32_t frameNumber, int numActiveFeatures,
+                                 torch::Tensor particleDensity,
+                                 torch::Tensor particleRadiance,
+                                 torch::Tensor rayOrigin,
+                                 torch::Tensor rayDirection,
+                                 torch::Tensor rayTimestamp,
+                                 TSensorModel sensorModel,
+                                 TTimestamp startTimestamp,
+                                 TTimestamp endTimestamp,
+                                 torch::Tensor sensorsStartPose,
+                                 torch::Tensor sensorsEndPose,
+                                 torch::Tensor objectMask,
+                                 int objectMaskNumObj) {
+
+    const int cudaDeviceIndex = rayOrigin.get_device();
+    cudaStream_t cudaStream   = at::cuda::getCurrentCUDAStream(cudaDeviceIndex);
+
+    const int width  = rayOrigin.size(2);
+    const int height = rayOrigin.size(1);
+
+    const uint32_t numParticles = particleDensity.size(0);
+
+    if (objectMaskNumObj <= 0) {
+        throw std::runtime_error("[3dgut] objectMaskNumObj must be > 0 in traceWithObjectMask");
+    }
+
+    if (objectMask.scalar_type() != torch::kInt32) {
+        throw std::runtime_error("[3dgut] objectMask tensor must be int32 in traceWithObjectMask");
+    }
+    if (!(objectMask.dim() == 2 && objectMask.size(0) == height && objectMask.size(1) == width)) {
+        throw std::runtime_error("[3dgut] objectMask must have shape [H, W] matching ray resolution");
+    }
+
+    const torch::TensorOptions opts = torch::TensorOptions().dtype(torch::kFloat32).device(torch::kCUDA);
+
+    torch::Tensor rayRadianceDensity = torch::zeros({height, width, 4}, opts);
+    torch::Tensor rayHitDistance     = torch::ones({height, width, 1}, opts).multiply(1e06f);
+    torch::Tensor rayHitCount        = torch::zeros({height, width, 1}, opts);
+    torch::Tensor particleVisibility = torch::zeros({numParticles, 1}, opts);
+    torch::Tensor objectGaussianAccum = torch::zeros({objectMaskNumObj, static_cast<int64_t>(numParticles)}, opts);
+
+    m_parameters.values.numParticles               = numParticles;
+    m_parameters.values.radianceSphDegree          = numActiveFeatures;
+    m_parameters.parameters.dptrDensityParameters  = voidDataPtr(particleDensity);
+    m_parameters.parameters.dptrRadianceParameters = voidDataPtr(particleRadiance);
+    m_parameters.valuesBuffer.setFromHost(&m_parameters.values, sizeof(m_parameters.values), reinterpret_cast<uint64_t>(cudaStream), m_logger);
+    m_parameters.parametersBuffer.setFromHost(&m_parameters.parameters, sizeof(m_parameters.parameters), reinterpret_cast<uint64_t>(cudaStream), m_logger);
+
+    std::shared_ptr<CudaTimer> timer;
+    if (m_enableKernelTimings) {
+        timer = std::make_shared<CudaTimer>("forward_render", cudaStream);
+        m_timers.emplace_back(timer);
+        if (m_timers.size() > m_maxNumTimers) {
+            m_timers.pop_front();
+        }
+    }
+
+    threedgut::RenderParameters renderParameters;
+    renderParameters.id          = frameNumber;
+    renderParameters.resolution  = tcnn::ivec2{width, height};
+    renderParameters.sensorModel = sensorModel;
+    renderParameters.sensorState = toSensorState(startTimestamp, sensorsStartPose, endTimestamp, sensorsEndPose);
+    renderParameters.objectAABB  = threedgut::BoundingBox{tcnn::vec3{-1e06f, -1e06f, -1e06f}, tcnn::vec3{1e06f, 1e06f, 1e06f}};
+
+    ErrorCode status = m_renderer->renderForward(
+        renderParameters,
+        reinterpret_cast<const tcnn::vec3*>(voidDataPtr(rayOrigin)),
+        reinterpret_cast<const tcnn::vec3*>(voidDataPtr(rayDirection)),
+        reinterpret_cast<float*>(voidDataPtr(rayHitCount)),
+        reinterpret_cast<float*>(voidDataPtr(rayHitDistance)),
+        reinterpret_cast<tcnn::vec4*>(voidDataPtr(rayRadianceDensity)),
+        reinterpret_cast<int*>(voidDataPtr(particleVisibility)),
+        reinterpret_cast<const int32_t*>(voidDataPtr(objectMask)),
+        objectMaskNumObj,
+        reinterpret_cast<float*>(voidDataPtr(objectGaussianAccum)),
+        m_parameters,
+        cudaDeviceIndex,
+        cudaStream);
+
+    CUDA_CHECK_LAST(m_logger);
+
+    if (timer) {
+        timer->stop();
+    }
+
+    return std::tuple<torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor, torch::Tensor>(
+        rayRadianceDensity,
+        rayHitDistance,
+        rayHitCount,
+        particleVisibility,
+        objectGaussianAccum);
+}
+
 std::tuple<torch::Tensor, torch::Tensor>
 SplatRaster::traceBwd(uint32_t frameNumber, int numActiveFeatures,
                       torch::Tensor particleDensity,
diff --git a/threedgut_tracer/tracer.py b/threedgut_tracer/tracer.py
index cb11f45..7a4747f 100644
--- a/threedgut_tracer/tracer.py
+++ b/threedgut_tracer/tracer.py
@@ -329,6 +329,8 @@ class Tracer:
 
             pred_rgb = pred_rgba[..., :3].unsqueeze(0).contiguous()
             pred_opacity = pred_rgba[..., 3:].unsqueeze(0).contiguous()
+            pred_transmittance = (1.0 - pred_opacity).contiguous()
+            pred_density = pred_opacity.contiguous()
             pred_dist = pred_dist.unsqueeze(0).contiguous()
             hits_count = hits_count.unsqueeze(0).contiguous()
 
@@ -341,6 +343,8 @@ class Tracer:
         return {
             "pred_rgb": pred_rgb,
             "pred_opacity": pred_opacity,
+            "pred_transmittance": pred_transmittance,
+            "pred_density": pred_density,
             "pred_dist": pred_dist,
             "pred_normals": torch.nn.functional.normalize(torch.ones_like(pred_rgb), dim=3),
             "hits_count": hits_count,
diff --git a/configs/render/threedgrut_flashsplat.yaml b/configs/render/threedgrut_flashsplat.yaml
new file mode 100644
index 0000000..bbb8b12
--- /dev/null
+++ b/configs/render/threedgrut_flashsplat.yaml
@@ -0,0 +1,20 @@
+# order in which configs override each other (/* - denotes a relative search path)
+defaults:
+  - 3dgut
+  - _self_
+
+method: threedgrut_flashsplat
+
+flashsplat:
+  # If true, returns FlashSplat auxiliary tensors in output dict.
+  return_aux: false
+  # Enables gradient masking path in flashsplat-rasterization.
+  mask_grad: false
+  # Number of object labels; include background as 0.
+  num_obj: 2
+  # Require object-id masks in batch when this renderer is active.
+  require_object_mask: true
+  # Optional global fallback path in case object masks are not present in dataset batches.
+  object_mask_path_override: null
+  # Optional per-gaussian integer label file path (.pt/.pth/.npy) used for selective mode.
+  unique_label_path: null
\ No newline at end of file
